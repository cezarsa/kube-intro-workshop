apiVersion: v1
kind: Pod
metadata:
  name: mylimitedpod2
spec:
  containers:
  - name: mypod1
    image: nginx
    resources:
      requests:
        memory: "128Mi"
        cpu: "500m"
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
    - containerPort: 80
